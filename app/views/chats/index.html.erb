<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>chatchat</title>
  </head>
  <body>
    <h2>つぶやき一覧</h2>
    <p><%= notice %></p>
    <table>
      <% @chats.each do |chat| %>
      <tr>
        <td><%= image_tag chat.user.image_icon %><%= chat.content %></td>
      </tr>
      <tr>
        <td><%= link_to '詳細',chat_path(chat.id), class: "btn btn-info"%></td>
        <% if chat.user_id == current_user.id %>
          <td><%= link_to '編集',edit_chat_path(chat.id) , data: { confirm: '本当に編集していいですか？' } ,class: "btn btn-info"%></td>
          <td><%= link_to '削除',chat_path(chat.id),method: :delete , data: { confirm: '本当に削除していいですか？'},class:"btn btn-danger" %></td>
        <% end %>
      </tr>
      <% end %>
    </table>
    </div>


  </body>
</html>
